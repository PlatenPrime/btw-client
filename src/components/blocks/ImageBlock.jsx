import React from 'react';

const ImageBlock = ({src, alt, width, className}) => {


	const style = `

	ImageBlock
	
	${className}

`



	return (
		<img className={style} >
			
		</img>
	);
};

export default ImageBlock;